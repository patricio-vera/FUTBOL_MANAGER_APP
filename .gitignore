# Dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files — NUNCA subir a git
# Patrón amplio: cubre .env, .env.local y cualquier variante/backup
# (ej. .env.local.neon-backup). .env.example es la única excepción,
# porque es la plantilla que sí se versiona.
.env*
!.env.example

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Prisma
/prisma/*.db
/prisma/*.db-journal

# Salidas de sesiones de Claude — no son parte del proyecto versionado
Claude outputs/
